
const { token } = require('./config.json');
const PREFIX = '/';
const NONE = '';
const VERSION = '2.1.8';
const BOT_NAME = 'Mesa';

module.exports = {
    PREFIX,
    NONE,
    VERSION,
    BOT_NAME,
    token
}